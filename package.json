{
  "name": "dsa-algo",
  "version": "1.0.0",
  "scripts": {
    "build:wasm": "cd cpp && make",
    "dev": "cd nextjs-app && npm run dev",
    "build": "npm run build:wasm && cd nextjs-app && npm run build",
    "start": "cd nextjs-app && npm start"
  }
}